spring:
  security:
    user:
      name: admin
      password: password
## default connection pool
  datasource:
    url: jdbc:postgresql://localhost:55004/postgres
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      # this can be used for testing
      ddl-auto: create-drop

    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5


#
#  # create and drop table, good for testing, production set to none or comment it
#  spring.jpa.hibernate.ddl-auto=create-drop

logging:
  level:
    root: info

management:
  endpoint:
    metrics:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

scrape_configs:
  - job_name: 'aerbac-actuator'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['localhost:8080']

springdoc:
  show-actuator: true








